import React from 'react'

import withForm, { withFormProps } from 'src/components/Form/withForm'

type Props = withFormProps

function ProfileSecurity (props : Props) {
  return (
    <>content</>
  )
}

export default withForm(ProfileSecurity)
